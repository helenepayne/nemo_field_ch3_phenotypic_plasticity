# R / IDE noise
.Rproj.user
.Rhistory
.RData
.Ruserdata
.DS_Store
*.html

# Knitr outputs
*_files/
*_cache/

# Compiled-sheet data CSVs (fetched from Dryad on first run via
# R/fetch_dryad.R, or populated manually while the package is embargoed).
# We do NOT ignore the entire data/ directory because data/aster_outputs/
# is vendored from chapter 1 and must remain tracked.
data/*.csv

# Data loaded by Rmds via here::here('data_sheets/compiled_sheets/...')
data_sheets/
